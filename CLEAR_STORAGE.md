# 🧹 Инструкция по очистке localStorage и Redis

## ✅ Redis очищен

Redis был очищен автоматически при перезапуске:
```bash
docker-compose exec redis redis-cli FLUSHALL
```

---

## 📱 Очистка localStorage (клиентская операция)

**localStorage** — это хранилище браузера, его нельзя очистить с сервера. 

### Способ 1: Через консоль браузера (рекомендуется)

1. Откройте сайт: http://localhost:5173
2. Нажмите `F12` (или `Ctrl+Shift+I` / `Cmd+Option+I` на Mac)
3. Перейдите во вкладку **Console**
4. Введите команду:
```javascript
localStorage.clear();
```
5. Нажмите `Enter`
6. Обновите страницу (`F5` или `Ctrl+R`)

---

### Способ 2: Через DevTools → Application

1. Откройте DevTools (`F12`)
2. Перейдите во вкладку **Application** (или **Хранилище**)
3. В левой панели найдите **Local Storage** → `http://localhost:5173`
4. Кликните правой кнопкой → **Clear** (или выделите все и нажмите `Delete`)
5. Обновите страницу

---

### Способ 3: Удалить конкретный ключ темы

Если нужно удалить только настройки темы:

```javascript
localStorage.removeItem('theme-preference');
```

---

## 🔄 После очистки

После очистки localStorage:
- Тема вернётся к автоматическому режиму
- Система определит тему по системным настройкам или времени суток
- Новые предпочтения будут сохраняться при следующем переключении

---

## ✅ Статус

- ✅ **Redis**: Очищен автоматически
- ⚠️ **localStorage**: Требует ручной очистки в браузере (см. выше)

---

**Все сервисы перезапущены и готовы к работе!** 🚀

