# Восстановление сервиса Litebrick

## 🚀 Быстрое восстановление (одна команда)

```bash
cd /root/app/litebricks-ai-landing && ./restart.sh
```

---

## ⚙️ Автозапуск при перезагрузке сервера

### Установка systemd сервиса:

```bash
# Копируем файл сервиса
sudo cp /root/app/litebricks-ai-landing/litebrick.service /etc/systemd/system/

# Перезагружаем systemd
sudo systemctl daemon-reload

# Включаем автозапуск
sudo systemctl enable litebrick

# Запускаем сервис
sudo systemctl start litebrick
```

### Управление сервисом:

```bash
# Статус
sudo systemctl status litebrick

# Перезапуск (с перечитыванием .env)
sudo systemctl restart litebrick

# Остановка
sudo systemctl stop litebrick

# Логи
sudo journalctl -u litebrick -f
```

---

## 🔧 Структура конфигурации

### Ключевые файлы:

| Файл | Описание |
|------|----------|
| `backend/.env` | Переменные окружения (Calendar ID, API ключи) |
| `backend/google-key.json` | JSON ключ Service Account для Google Calendar |
| `docker-compose.yml` | Конфигурация контейнеров |

### Важные переменные в `backend/.env`:

```env
# Google Calendar
GOOGLE_CALENDAR_ID=xxx@group.calendar.google.com
GOOGLE_SERVICE_ACCOUNT_EMAIL=xxx@xxx.iam.gserviceaccount.com

# База данных (не менять!)
DATABASE_URL=postgresql://booking_user:booking_pass@postgres:5432/booking_db
```

---

## ⚠️ Почему пропадает интеграция

**Причина:** Docker кэширует переменные окружения при создании контейнера.

**Решение:** Всегда используйте `docker-compose down && up` вместо `restart`:

```bash
# ❌ НЕПРАВИЛЬНО (не перечитывает .env)
docker-compose restart

# ✅ ПРАВИЛЬНО (перечитывает .env)
docker-compose down && docker-compose up -d
```

Скрипт `restart.sh` делает это автоматически.

---

## 🔍 Диагностика проблем

### 1. Проверить здоровье backend:
```bash
curl http://localhost:3001/health
```

### 2. Проверить Google Calendar:
```bash
docker-compose exec backend node test-calendar.js
```

### 3. Проверить переменные в контейнере:
```bash
docker-compose exec backend env | grep GOOGLE
```

### 4. Логи backend:
```bash
docker-compose logs -f backend
```

---

## 📋 Контрольный список после восстановления

- [ ] Backend отвечает на `/health`
- [ ] Frontend загружается
- [ ] Google Calendar подключен (test-calendar.js)
- [ ] Брони создаются с `googleEventId`
- [ ] События появляются в Google Calendar

---

## 📞 Контакты Service Account

**Email для предоставления доступа к календарю:**
```
booking-consultation@booking-system-478111.iam.gserviceaccount.com
```

При создании нового календаря — добавьте этот email с правами "Внесение изменений в мероприятия".




