# Руководство по работе с часовыми поясами (UTC+3 МСК)

## 📍 Основные принципы

Система бронирования работает **исключительно в московском часовом поясе (UTC+3)**.

Все временные данные:
- ✅ Вводятся пользователем в **московском времени** (UTC+3)
- ✅ Сохраняются в базу данных в **UTC** (стандарт PostgreSQL)
- ✅ Отображаются пользователю в **московском времени** (UTC+3)

---

## 🔄 Жизненный цикл времени

### 1. Ввод данных (Фронтенд)

**Файл:** `src/components/Booking.jsx`

Пользователь выбирает:
- Дата: `2025-11-15`
- Время: `10:00`

Система создает:
```javascript
// Явное указание московского часового пояса
const moscowTimeString = `2025-11-15T10:00:00+03:00`;
const startDate = new Date(moscowTimeString);

// Конвертация в UTC для отправки на сервер
const startUtc = startDate.toISOString(); // "2025-11-15T07:00:00.000Z"
```

**Результат:** `10:00 МСК → 07:00 UTC`

---

### 2. Обработка на сервере (Бэкенд)

**Файл:** `backend/src/routes/bookings.js`

Получаем UTC время:
```javascript
const startUtc = "2025-11-15T07:00:00.000Z"; // из запроса

// Парсим как UTC
const startTime = DateTime.fromISO(startUtc, { zone: 'utc' });

// Конвертируем в московское для проверок
const startMoscow = startTime.setZone('Europe/Moscow'); // 10:00 МСК
```

**Проверки:**
1. Доступность в Google Calendar (в UTC)
2. Проверка в базе данных (в UTC)
3. Минимальное время бронирования (2 часа вперед в МСК)

---

### 3. Сохранение в БД

**Файл:** `backend/src/routes/bookings.js`

```javascript
const booking = await prisma.booking.create({
  data: {
    date: dateObj,           // Дата (начало дня в МСК, конвертированное в UTC)
    startUtc: startTime.toJSDate(), // 07:00 UTC (10:00 МСК)
    endUtc: endTime.toJSDate(),     // 08:00 UTC (11:00 МСК)
    // ...
  },
});
```

**В PostgreSQL сохраняется:**
- `date`: `2025-11-15T00:00:00.000Z` (начало дня по МСК в UTC)
- `startUtc`: `2025-11-15T07:00:00.000Z` (10:00 МСК в UTC)
- `endUtc`: `2025-11-15T08:00:00.000Z` (11:00 МСК в UTC)

---

### 4. Отображение (Фронтенд)

**Файл:** `src/components/Preparation.jsx`

```javascript
const formatDateTime = (dateString) => {
  const date = new Date(dateString); // "2025-11-15T07:00:00.000Z" (UTC)
  
  return date.toLocaleString('ru-RU', {
    timeZone: 'Europe/Moscow', // Конвертация в МСК
    // ...
  });
  // Результат: "15 ноября 2025 г., 10:00"
};
```

**Результат:** `07:00 UTC → 10:00 МСК`

---

## 📅 Работа с доступностью

**Файл:** `backend/src/routes/availability.js`

### Генерация временных слотов

```javascript
// Дата интерпретируется как московская
const dateMoscow = DateTime.fromISO('2025-11-15').setZone('Europe/Moscow');

// Рабочие часы в московском времени
const workStart = dateMoscow.set({ hour: 10, minute: 0 }); // 10:00 МСК
const workEnd = dateMoscow.set({ hour: 18, minute: 0 });   // 18:00 МСК

// Генерация слотов с шагом 60 минут
let current = workStart;
while (current < workEnd) {
  const slotStart = current.toUTC(); // Конвертация в UTC для хранения
  const slotEnd = current.plus({ minutes: 60 }).toUTC();
  
  slots.push({
    startUtc: slotStart.toISO(),
    endUtc: slotEnd.toISO(),
  });
  
  current = current.plus({ minutes: 60 });
}
```

**Результат:** Слоты генерируются в МСК, отправляются в UTC

### Пример ответа API

```json
[
  {
    "startUtc": "2025-11-15T07:00:00.000Z",
    "endUtc": "2025-11-15T08:00:00.000Z"
  }
]
```

Что соответствует **10:00-11:00 МСК**

---

## 🎯 Проверка корректности

### Консольные логи

#### Фронтенд (Booking.jsx)
```javascript
console.log('Выбранное пользователем время (МСК):', {
  date: '2025-11-15',
  time: '10:00',
});

console.log('Время конвертировано в UTC для отправки:', {
  startUtc: '2025-11-15T07:00:00.000Z',
  endUtc: '2025-11-15T08:00:00.000Z',
});
```

#### Бэкенд (bookings.js)
```javascript
logger.info({
  requestedTime: {
    moscow: '2025-11-15 10:00 МСК',
    utc: '2025-11-15T07:00:00.000Z',
  }
}, 'Создание брони');

logger.info({
  bookingId: 123,
  time: '2025-11-15 10:00 - 11:00 МСК',
  contact: 'user@example.com',
}, 'Бронь успешно создана');
```

---

## ⚠️ Важные замечания

### 1. Почему UTC в базе данных?

- ✅ **Стандарт PostgreSQL**: хранение в UTC - общепринятая практика
- ✅ **Независимость от часового пояса**: данные корректны для любой временной зоны
- ✅ **Летнее/зимнее время**: автоматически учитывается при конвертации
- ✅ **Консистентность**: все временные метки в единой базе (UTC)

### 2. Конвертация времени

```javascript
// ❌ НЕПРАВИЛЬНО: сохранять московское время напрямую
startUtc: '2025-11-15T10:00:00.000Z' // Это 13:00 МСК, а не 10:00!

// ✅ ПРАВИЛЬНО: конвертировать МСК → UTC
const moscowTime = '2025-11-15T10:00:00+03:00';
const utcTime = new Date(moscowTime).toISOString(); // '2025-11-15T07:00:00.000Z'
```

### 3. Отображение времени

```javascript
// ❌ НЕПРАВИЛЬНО: показывать UTC время
'07:00' // Пользователь не поймет

// ✅ ПРАВИЛЬНО: конвертировать в МСК
date.toLocaleString('ru-RU', { timeZone: 'Europe/Moscow' }) // '10:00'
```

---

## 🔍 Отладка

### Проверка конвертации

```bash
# Проверить доступные слоты для даты
curl "http://localhost:3001/api/availability?date=2025-11-15"

# Проверить созданную бронь
curl "http://localhost:3001/api/bookings/123"
```

### Консольные команды

```javascript
// В консоли браузера
const moscowTime = '2025-11-15T10:00:00+03:00';
const date = new Date(moscowTime);

console.log('UTC:', date.toISOString()); // 2025-11-15T07:00:00.000Z
console.log('МСК:', date.toLocaleString('ru-RU', { timeZone: 'Europe/Moscow' })); // 15.11.2025, 10:00
```

---

## 📊 Схема потока данных

```
┌─────────────────────────────────────────────────────────────────┐
│                        ПОЛЬЗОВАТЕЛЬ                             │
│                                                                 │
│                    Выбирает: 10:00 МСК                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   ФРОНТЕНД (Booking.jsx)                        │
│                                                                 │
│  1. Создает: 2025-11-15T10:00:00+03:00                        │
│  2. Конвертирует в UTC: 2025-11-15T07:00:00.000Z             │
│  3. Отправляет на сервер                                       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   БЭКЕНД (bookings.js)                          │
│                                                                 │
│  1. Получает UTC: 2025-11-15T07:00:00.000Z                    │
│  2. Проверяет доступность (в UTC)                             │
│  3. Конвертирует в МСК для логов: 10:00 МСК                   │
│  4. Сохраняет в БД (в UTC)                                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                  БАЗА ДАННЫХ (PostgreSQL)                       │
│                                                                 │
│  startUtc: 2025-11-15T07:00:00.000Z (UTC)                     │
│  endUtc:   2025-11-15T08:00:00.000Z (UTC)                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│               ОТОБРАЖЕНИЕ (Preparation.jsx)                     │
│                                                                 │
│  1. Читает из БД: 2025-11-15T07:00:00.000Z (UTC)             │
│  2. Конвертирует в МСК: 15 ноября 2025 г., 10:00             │
│  3. Показывает пользователю                                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        ПОЛЬЗОВАТЕЛЬ                             │
│                                                                 │
│                    Видит: 10:00 МСК ✅                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## ✅ Контрольный список

- [x] Пользователь вводит время в московском часовом поясе
- [x] Фронтенд явно указывает `+03:00` при создании даты
- [x] Бэкенд парсит время с зоной `utc`
- [x] База данных хранит время в UTC
- [x] API возвращает время в UTC
- [x] Отображение конвертирует в московское время через `timeZone: 'Europe/Moscow'`
- [x] Логи показывают время в московском часовом поясе для удобства
- [x] Доступность проверяется в московском рабочем времени (10:00-18:00 МСК)

---

## 🎓 Дополнительные ресурсы

- [Luxon Documentation](https://moment.github.io/luxon/)
- [MDN: Date.prototype.toLocaleString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toLocaleString)
- [PostgreSQL: Date/Time Types](https://www.postgresql.org/docs/current/datatype-datetime.html)
- [IANA Time Zone Database](https://www.iana.org/time-zones)

